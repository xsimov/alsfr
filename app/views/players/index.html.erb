<h1>RellotgesDeLaParetDelGranSal&oacute;DeLaSeuNoEstivalDeLALSFr</h1>

<table class="table table-responsive table-striped table-hover">
  <thead>
    <tr>
      <th>Posici&oacute;</th>
      <th>Nom</th>
      <th>N&uacute;mero de Badges</th>
      <th>Punts totals</th>
      <th>Accions</th>
    </tr>
  </thead>
  <tbody class="">
    <% @players.each.with_index do |player, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= player.name %></td>
        <td><%= player.badges.count %></td>
        <td><%= player.total_points %></td>
        <td><%= link_to "Editar", edit_player_path(player) %> | <%= link_to "Eliminar", player_path(player), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>